html
html
    head
        title Form Tester
    body
        form(action = "pug/pug_mess", method = "POST")
            div 
                label(for = "say") Say:
                input(name = "say" value = "Hi")
            br
            div
                label(for = "to") To:
                input#idname(name = "to" value "Express forms")
            br
            button(type = "submit") Send